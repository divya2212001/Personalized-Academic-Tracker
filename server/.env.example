# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# Replace <password> with your actual password and <dbname> with your database name
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT Configuration
# You can generate a random secret using: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Client URL (for CORS and email links)
CLIENT_URL=http://localhost:5173

# Email Configuration (for verification emails)
# Using Gmail? You might need an App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password
